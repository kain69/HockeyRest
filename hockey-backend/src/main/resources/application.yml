spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hockeyrestdb
    username: postgres
    password: postgres
  jpa:
    generate-ddl: 'true'
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true